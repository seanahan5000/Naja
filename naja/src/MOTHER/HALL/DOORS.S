DOOR_TEXTS		JSR	STEP_SUB
				JSR	DRAW_SIGN
				LDA	#WHITE1
				STA	COLOR
				LDA	COUNTER
				CMP	#$14
				BNE	:LOOP2
:LOOP1			LDA	KEYBRD
				BPL	:LOOP1
				JSR	CPARER2
				BEQ	:SKIPA
				BIT	UNSTROB
				JMP	:LOOP1

:LOOP2			LDA	KEYBRD
				BPL	:LOOP2
				JSR	CPARER1
				BEQ	:SKIPA
				BIT	UNSTROB
				JMP	:LOOP2
:SKIPA			PLA
				PLA
				JMP	RDKEY

DRAW_SIGN		LDY	COUNTER
				LDA	DVERT,Y
				PHA
				TAY
				LDA	COLORS,Y
				STA	COLOR
				JSR	LINER
				PLA
				TAY
				JMP	TEXTER

DVERT			DB	$03,$00,$01
				DB	$FF,$01,$FF
				DB	$02,$FF,$FF
				DB	$0A,$FF,$05
				DB	$02,$03,$FF
				DB	$FF,$07,$00
				DB	$FF,$04,$08
				DB	$FF,$09,$02
				DB	$03,$04,$EA
				DB	$EA,$0B

COLORS			DB	RED,BLUE,BLACK1
				DB	BLACK1,VIOLET,BLUE
				DB	VIOLET,GREEN,RED
				DB	VIOLET,VIOLET,BLUE

				do	0				;***
STEP_SUB		LDX	FLAG12
				DEX
				BEQ	:SKIPA
				JMP	SCANONE
:SKIPA			JSR	CLEAR1
				JMP	NOSCAN
				else				;***
STEP_SUB		JSR	SCANONE
TO_PRIMARY		LDA	#$20
				CMP	PAGE
				BEQ	:1
				STA	PAGE
				JSR	MOVETO1
				STA	PRIMARY
:1				RTS
				fin					;***
