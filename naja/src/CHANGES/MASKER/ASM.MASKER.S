				LST	OFF
				TR	ON

TEMP			=	$00				;$01
DESTINL			=	$0D
DESTINH			=	$0E
SCREENL			=	$26
SCREENH			=	$27

				ORG	$6000

MASKER			STA	$C055
				JSR	CLEAR2			;TEMP
				JSR	MAKE_MASK
				JSR	INVERT_MASK
				JSR	THICKEN_MASK
				STA	$C054			;TEMP
				JSR	TEST_MASK		;TEMP
				RTS

				PUT	TEST.MASK		;TEMP
				PUT	SCAN.HORZ
				PUT	INVERT.MASK
				PUT	THICKEN.MASK
				PUT	HIRES.TABLES
				SAV	MASKER
